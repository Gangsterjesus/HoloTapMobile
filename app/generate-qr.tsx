import { View, Text } from "react-native";

export default function GenerateQRScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontSize: 24 }}>Generate QR Screen</Text>
    </View>
  );
}
